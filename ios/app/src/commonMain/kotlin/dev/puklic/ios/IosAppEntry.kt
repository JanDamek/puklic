package dev.puklic.ios

/**
 * Phase 1 stub entry point. Phase 2 will return a real `UIViewController` hosting Compose Multiplatform.
 * Kept as a pure-Kotlin marker so the module compiles on iosArm64/iosX64/iosSimulatorArm64 without
 * pulling in UIKit cinterop yet.
 */
@Suppress("FunctionOnlyReturningConstant")
fun puklicIosEntryStub(): String = "puklic-ios-phase1"
